class Car {
	park() {
		return 'stopped'; 
	}
	drive() {
		return 'vroom';
	}
}

let car;

beforeEach(() => {
	car = new Car();
});

describe('Car', () => {
	it('Park Test', () => {
		//const car = new Car();
		assert.equal(car.park(), 'stopped');
	});
	it('Drive Test', () => {
		//const car = new Car();
		assert.equal(car.drive(), 'vroom');
	});
})